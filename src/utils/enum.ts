export enum Operator {
  ADD = '+',
  MULTIPLY = '*',
  SUBTRACT = '-',
  DIVIDE = '/',
  SET = '=',
}

export enum ESceneType {
  DIALOGUE = 'dialogue',
  COMBAT = 'combat',
  EXPLORATION = 'exploration',
  EVENT = 'event',
  SHOP = 'shop',
  OTHER = 'other',
}

export enum EEncounterType {
  COMBAT = 'combat',
}
