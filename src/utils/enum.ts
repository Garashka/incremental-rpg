export enum Operator {
  ADD = '+',
  MULTIPLY = '*',
  SUBTRACT = '-',
  DIVIDE = '/',
}

export enum ESceneType {
  DIALOGUE = 'dialogue',
  COMBAT = 'combat',
  EXPLORATION = 'exploration',
  EVENT = 'event',
  SHOP = 'shop',
  OTHER = 'other',
}
